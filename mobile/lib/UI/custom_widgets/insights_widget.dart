import 'package:flutter/material.dart';

class InsightsWidget extends StatelessWidget {
  const InsightsWidget({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Text('at insights');
  }
}